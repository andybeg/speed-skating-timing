# ТЗ: система старта и финиша для скоростных роликов

Мануал / техническое задание на DIY-систему хронометража: пересечение линии, старт (масс / по одному), фото-финиш, задел под RFID.

**Центральный контроллер:** Raspberry Pi 5 8 GB (единый узел — Hub + фото-финиш).

**Версия:** 0.4.2  
**Дата:** 2026-05-23  
**Статус:** черновик

---

## 1. Назначение

Полевая система фиксации **старта** и **финиша** на соревнованиях по скоростным роликам с:

- пересечением финишной линии по полосам;
- двумя режимами старта: **масс-старт** и **по одному**;
- **управлением и заданием heats** с **смартфона** или **компьютера** (web-UI + API);
- **фото-финишем** на том же **Raspberry Pi 5**;
- **гибкой связью** Pi 5 ↔ ESP32: **провод** (UART / RS485) и **WiFi**;
- заделом под **RFID** без переделки архитектуры.

---

## 2. Состав системы

| Блок | Платформа | Назначение |
|------|-----------|------------|
| **Central (Hub + Photo)** | Raspberry Pi 5 8 GB + Pi Camera 3 | Протокол, WiFi, UI, SQLite, 120 fps, фото-финиш |
| **Start Unit** | ESP32-S3 | Отсчёт, сигнал, фиксация старта |
| **Finish Unit** | ESP32-S3 | IR-барьеры по полосам, timestamp пересечения |
| **Клиент** | Смартфон / ноутбук / планшет | Управление Pi 5, heats, live, фото-финиш |
| **Опция: RFID** | UHF-ридер | Финишная рама, USB → Pi 5 |

---

## 3. Архитектура

```
                    WiFi или UART/RS485           ┌──────────────────────────┐
┌─────────────┐ ────────────────────────────────►│                          │
│ Start Unit  │                                  │   Raspberry Pi 5 8 GB    │
│  ESP32-S3   │                                  │   • NTP master           │
└─────────────┘                                  │   • FastAPI + web-UI     │
                                                 │   • SQLite               │
┌─────────────┐   UART + GPIO (обязательно)      │   • picamera2 120 fps    │
│Finish Unit  │ ────────────────────────────────►│   • WiFi AP              │
│  ESP32-S3   │                                  │   • ring buffer          │
└─────────────┘                                  └────────────┬─────────────┘
                                                              │ CSI
                                                 ┌────────────▼─────────────┐
                                                 │  Pi Camera Module 3    │
                                                 └────────────────────────┘

[Опция] UHF RFID ──USB──► Pi 5
[Клиент] Smartphone / PC ──WiFi / USB / LAN──► Pi 5
         • web-UI (управление, heats, live)
         • REST API + WebSocket
```

### Разделение ролей

| Узел | Задача |
|------|--------|
| **Pi 5** | Единый центр: события, время, видео, сеть, экспорт |
| **Finish ESP32** | µs-точность IR; **только провод** (UART + GPIO) на Pi 5 |
| **Start ESP32** | Отсчёт и `START`; **провод или WiFi** — см. §3.1 |

**Синхронизация:** Pi 5 = единственный master времени. ESP32 по UART — sync-pulse 1 Hz. Start по WiFi — периодическая калибровка offset.

### 3.1. Связь Pi 5 ↔ ESP32 (провод / WiFi)

Система поддерживает **несколько вариантов подключения** ESP32. Выбор задаётся при монтаже и в настройках (`transport` в web-UI).

#### Finish Unit — только провод

| Параметр | Значение |
|----------|----------|
| Интерфейс | **UART 115200** + **GPIO trigger** (обязательно) |
| Длина кабеля | 3–10 m ( витая пара или экранированная 4–6 жил) |
| Подключение к Pi 5 | USB-UART (CH340 / CP2102) + GPIO pin |
| WiFi | **не используется** (надёжность финиша и фото-финиша) |

GPIO нужен для мгновенной привязки IR-события к кадру камеры; через WiFi это не заменяется.

#### Start Unit — провод или WiFi

| Режим | Когда | Длина | Подключение |
|-------|-------|-------|-------------|
| **UART** (рекомендуется) | Старт рядом с финишем | ≤ 10 m | USB-UART на Pi 5 |
| **RS485** (опция) | Старт далеко, но нужен провод | 50–300 m | Витая пара, RS485-TTL на ESP32 и Pi 5 |
| **WiFi** (fallback) | Старт далеко, кабель неудобен | ≤ 50 m (прямая видимость) | ESP32-S3 → WiFi AP Pi 5 |

**Dual-mode в прошивке Start Unit:** поддержка `uart` и `wifi`; переключение через web-UI или GPIO-джumper. При обрыве связи — локальный буфер событий (≥ 100).

#### Сводная таблица транспорта

| Узел | UART | GPIO | WiFi | RS485 |
|------|------|------|------|-------|
| **Finish** | ✓ обязательно | ✓ обязательно | ✗ | опция v2 |
| **Start** | ✓ приоритет | — | ✓ fallback | опция v2 |

#### Выбор режима под площадку

| Сценарий | Finish | Start |
|----------|--------|-------|
| Спринт, старт = финиш | UART + GPIO | **UART** |
| Критериум, старт ≤ 10 m | UART + GPIO | **UART** |
| Кольцо, старт 30–100 m | UART + GPIO | **RS485** или **WiFi** |
| Быстрый перенос между площадками | UART + GPIO | **WiFi** |

#### Требования к кабельной инфраструктуре (TR)

| ID | Требование |
|----|------------|
| TR-01 | Finish Unit → Pi 5: **только провод** (UART + GPIO + общий GND) |
| TR-02 | Start Unit: **UART приоритетнее WiFi**, если кабель ≤ 10 m |
| TR-03 | На Pi 5: **2× USB-UART** (отдельный адаптер на каждый ESP32 при проводном режиме) |
| TR-04 | Кабели полевые: витая пара или мультикор в **защитном канале** / под лентой |
| TR-05 | Единый протокол JSON-lines на UART, WiFi и RS485 |
| TR-06 | Web-UI `/api/status`: `finish_link`, `start_link` = `uart \| wifi \| rs485 \| offline` |
| TR-07 | RS485 — **фаза v2**; в MVP достаточно UART (Finish + Start) и WiFi (Start) |
| TR-08 | Буфер событий на ESP32 при потере связи (≥ 100), replay после reconnect |

#### Схема проводного подключения (MVP)

```
Pi 5
 ├── USB-UART #1 ── TX/RX/GND ──► Finish ESP32
 │                                 └── GPIO ──► Pi 5 (trigger)
 └── USB-UART #2 ── TX/RX/GND ──► Start ESP32   (если transport=uart)
```

---

## 4. Функциональные требования

### 4.1. Старт

| ID | Требование |
|----|------------|
| ST-01 | **Масс-старт:** общий отсчёт 3–2–1–GO для всего heat |
| ST-02 | **По одному:** индивидуальный отсчёт; у каждого участника свой `t_start` |
| ST-03 | Звук (зуммер) + свет (LED-матрица / 7-segment) |
| ST-04 | Двухэтапный запуск: **Arm** → **Start** |
| ST-05 | Событие `START`: `{ts_us, heat_id, mode, bib?}` |
| ST-06 | В режиме «по одному» — выбор участника (bib) на клиенте до Arm |
| ST-07 | Start Unit → Pi 5: **UART** (приоритет), **WiFi** или **RS485** — см. §3.1 |
| ST-08 | Буфер событий на ESP32 при потере связи (≥ 100), replay после reconnect |

### 4.2. Финиш

| ID | Требование |
|----|------------|
| FN-01 | **2–4 полосы**, независимая фиксация пересечения линии |
| FN-02 | IR-барьер на полосу (940 nm LED + фототранзистор), высота 5–15 см |
| FN-03 | Событие `FINISH`: `{ts_us, lane, heat_id, seq}` |
| FN-04 | Расчёт времени: `t_finish - t_start` |
| FN-05 | Live-таблица на смартфоне, задержка ≤ 1 с |
| FN-06 | Экспорт CSV / JSON (WiFi или USB MSC) |
| FN-07 | Finish Unit → Pi 5 **только провод**: UART (≤ 10 m) + **GPIO trigger** |

### 4.3. Фото-финиш (на Pi 5)

| ID | Требование |
|----|------------|
| PH-01 | **RPi 5 8 GB** + **Pi Camera Module 3** |
| PH-02 | Захват ≥ **120 fps** @ 640×480 |
| PH-03 | Ring buffer ≥ **10 с** в RAM |
| PH-04 | Триггер: **GPIO** от Finish Unit → мгновенная привязка к `frame_idx` |
| PH-05 | При `FINISH` — сохранение clip ±2 с + ключевые кадры на SD |
| PH-06 | Фото-финиш **в том же web-UI** (вкладка / panel), прокрутка кадров |
| PH-07 | Overlay: вертикальная линия финиша |
| PH-08 | Камера **строго 90°** к линии; фиксированный фокус |
| PH-09 | При расхождении IR и кадра — **решение судьи** (`final_source: line \| photo`) |

### 4.4. Подключение клиентов к Pi 5

| ID | Требование |
|----|------------|
| CL-01 | Pi 5 как **WiFi AP** по умолчанию (`timing-race`, без интернета) |
| CL-02 | Режим **WiFi client** — подключение Pi 5 к существующей сети площадки |
| CL-03 | Доступ по **HTTP**: `http://timing.local` (mDNS) или IP Pi 5 |
| CL-04 | **USB**: режим RNDIS / gadget — ноутбук видит Pi 5 как сетевое устройство (fallback) |
| CL-05 | **Ethernet** (опционально): судейский ноутбук по кабелю |
| CL-06 | ≥ **3 одновременных клиента** без деградации live-обновлений |
| CL-07 | Клиенты **не хранят** протокол локально — источник истины только Pi 5 (SQLite) |

### 4.5. Управление Pi 5 и задание heats (смартфон / компьютер)

#### Роли пользователей

| Роль | Смартфон | Компьютер | Действия |
|------|----------|-----------|----------|
| **admin** | ✓ | ✓ | Настройки системы, WiFi, калибровка, пользователи |
| **secretary** | ✓ | ✓ (удобнее) | Соревнование, участники, heats, импорт CSV |
| **starter** | ✓ | ✓ | Активация heat, Arm / Start, режим mass / individual |
| **judge** | ✓ | ✓ (удобнее) | Фото-финиш, правка результатов, спорные финиши |
| **viewer** | ✓ | ✓ | Только live-таблица и опубликованные результаты |

Авторизация: PIN или простой пароль на сессию (поле без интернета). Роль выбирается при входе или по PIN.

#### Сущности (задания для Pi 5)

| Сущность | Описание |
|----------|----------|
| **Event** | Соревнование / день (название, дата, число полос) |
| **Category** | Дистанция, возраст, пол (`500m`, `U14` …) |
| **Participant** | Участник: `bib`, ФИО, клуб, RFID uid (позже) |
| **Heat** | **Задание** для системы: заезд с составом, полосами, режимом старта |
| **Result** | Время, место, источник (`line` / `photo`), статус (`provisional` / `final`) |

#### Управление heats (CRUD с клиента)

| ID | Требование |
|----|------------|
| HM-01 | Создание / редактирование / удаление **Event**, **Category**, **Heat** |
| HM-02 | Назначение участников на **полосы** (drag-and-drop на ПК; списки на телефоне) |
| HM-03 | Режим heat: `mass` или `individual` — задаётся до активации |
| HM-04 | **Очередь heats** — список «предстоящих / активный / завершённых» |
| HM-05 | **Activate heat** — Pi 5 отправляет конфиг на Start / Finish ESP32 |
| HM-06 | Только **один активный heat** одновременно |
| HM-07 | Дублирование heat, шаблоны (mass 4 lanes, individual sprint …) |
| HM-08 | Импорт участников **CSV** с компьютера; ручной ввод bib на смартфоне |
| HM-09 | Экспорт протокола **CSV / JSON / PDF** (PDF — v2) |

#### Команды управления системой (с клиента → Pi 5)

| ID | Команда | Кто | Описание |
|----|---------|-----|----------|
| CM-01 | `arm_start` | starter | Подготовка старта (ожидание GO) |
| CM-02 | `go_start` | starter | Запуск отсчёта 3–2–1–GO |
| CM-03 | `cancel_start` | starter | Отмена до GO |
| CM-04 | `finish_heat` | secretary / judge | Закрыть heat, зафиксировать результаты |
| CM-05 | `abort_heat` | admin | Сброс heat без сохранения |
| CM-06 | `correct_result` | judge | Ручная правка времени / места + комментарий |
| CM-07 | `open_photo_finish` | judge | Открыть clip по lane / heat |
| CM-08 | `set_active_bib` | starter | Для individual: текущий участник перед Arm |
| CM-09 | `system_status` | admin | Температура, FPS, ESP32 online, uptime |

Команды `arm_start` / `go_start` дублируются **физическими кнопками** на Start Unit; клиент и кнопки синхронизируются через Pi 5.

#### Web-UI: экраны

**Смартфон (mobile-first)**

| Экран | Содержание |
|-------|------------|
| Live | Таблица результатов активного heat, автообновление |
| Start | Активный heat, режим, кнопки Arm / GO / Cancel |
| Queue | Следующие heats, кнопка «Activate» |
| Quick bib | Быстрое добавление участника и назначение полосы |
| Photo | Просмотр кадров спорного финиша (упрощённый) |

**Компьютер**

| Экран | Содержание |
|-------|------------|
| Dashboard | Статус Pi 5, ESP32, камера, активный heat |
| Heats editor | Полный редактор: полосы, участники, режим |
| Participants | Список, импорт CSV, поиск |
| Results | Протокол, правки, экспорт |
| Photo finish | Прокрутка кадров, overlay линии, выбор места |
| Settings | WiFi AP/client, полосы, длительность отсчёта, PIN |

#### API Pi 5 (для web-UI и внешних клиентов)

REST (JSON):

| Метод | Путь | Назначение |
|-------|------|------------|
| GET | `/api/status` | Состояние системы |
| GET/POST | `/api/events` | Список соревнований |
| GET/POST/PATCH/DELETE | `/api/heats` | CRUD heats |
| POST | `/api/heats/{id}/activate` | Активировать heat |
| POST | `/api/heats/{id}/arm` | Arm start |
| POST | `/api/heats/{id}/go` | GO |
| POST | `/api/heats/{id}/cancel` | Отмена старта |
| GET/POST | `/api/participants` | Участники |
| POST | `/api/participants/import` | CSV |
| GET | `/api/results?heat_id=` | Результаты |
| PATCH | `/api/results/{id}` | Правка судьи |
| GET | `/api/photo/{heat_id}/frames` | Кадры фото-финиша |

WebSocket:

| Канал | События |
|-------|---------|
| `/ws/live` | `FINISH`, `START`, обновление таблицы, смена активного heat |
| `/ws/system` | ESP32 online/offline, temp, fps (для dashboard) |

Пример команды активации heat:

```json
POST /api/heats/H001/activate
{
  "mode": "mass",
  "lanes": [
    {"lane": 1, "bib": 12},
    {"lane": 2, "bib": 34},
    {"lane": 3, "bib": 56},
    {"lane": 4, "bib": 78}
  ]
}
```

Pi 5 рассылает конфиг Start / Finish ESP32 и переводит heat в статус `active`.

#### Сценарий: секретарь + стартер + судья

1. **Секретарь (ноутбук):** создаёт Event, импортирует CSV, формирует heats в очереди.
2. **Стартер (смартфон):** выбирает heat → Activate → Arm → GO.
3. **Pi 5:** принимает FINISH, считает время, пушит в `/ws/live`.
4. **Судья (планшет/ноутбук):** при споре — Photo finish → подтверждает места → `final`.
5. **Секретарь:** экспорт протокола после блока heats.

### 4.6. Расширение (RFID)

| ID | Требование |
|----|------------|
| EX-01 | Единый формат событий: `START \| FINISH \| RFID \| PHOTO` |
| EX-02 | Место под UHF patch-антенну (868 MHz) в финишной раме |
| EX-03 | RFID → USB на Pi 5, сопоставление uid ↔ bib |
| EX-04 | При конфликте RFID / line / photo — решение судьи |

---

## 5. Нефункциональные требования

| Параметр | Значение |
|----------|----------|
| Точность финиша (IR) | ≤ 5 ms |
| Точность фото-финиша (кадр) | ≤ 8 ms @ 120 fps |
| Полосы | 2–4 |
| Питание Pi 5 | 5 V **5 A** USB-C; powerbank 20 000 mAh (PD) или 12 V → DC-DC |
| Outdoor | IP65-бокс, **активное** охлаждение Pi 5 |
| Автономность | ≥ 4 ч |
| Готовность после включения | ≤ 2 мин |
| Офлайн | Полная работа без интернета |

---

## 6. Протокол событий

```json
{
  "type": "START|FINISH|RFID|PHOTO",
  "ts_us": 1234567890123,
  "heat_id": "H001",
  "lane": 2,
  "bib": 42,
  "mode": "mass|individual",
  "src": "start_unit|finish_unit|rfid|photo",
  "payload": {}
}
```

### Транспорт

| Связь | Протокол |
|-------|----------|
| Finish ESP32 → Pi 5 | **UART** 115200, JSON-lines + **GPIO trigger** (обязательно) |
| Start ESP32 → Pi 5 | **UART** 115200 (приоритет) или **WiFi** MQTT/HTTP или **RS485** (v2) |
| Pi 5 → Finish ESP32 | UART: конфиг heat, sync-pulse 1 Hz |
| Pi 5 → Start ESP32 | UART / WiFi / RS485: конфиг heat, команды arm/go |
| Pi 5 → клиент | REST API + WebSocket / SSE |
| RFID → Pi 5 | USB serial |

**Sync-pulse:** Pi 5 → ESP32 по UART, 1 Hz (калибровка drift). На Start в режиме WiFi — дополнительно HTTP ping раз в 10 с.

### Команды клиента → Pi 5

```json
{
  "cmd": "activate|arm|go|cancel|finish_heat|correct_result",
  "heat_id": "H001",
  "bib": 42,
  "payload": {}
}
```

Ответ Pi 5 — `{ "ok": true, "state": "armed|running|finished" }` или код ошибки.

---

## 7. Сценарии

### 7.1. Масс-старт

1. Секретарь создаёт heat на смартфоне (WiFi → Pi 5).
2. Стартер: Arm → 3-2-1-GO → `START`.
3. Финиши по полосам → расчёт времени.
4. Плотный финиш → судья открывает фото-финиш в том же UI.

### 7.2. Старт по одному

1. Выбор bib → Arm → GO → `START` с `bib`.
2. Финиш привязывается к участнику.

### 7.3. Спорный финиш

1. IR: `FINISH` lane 2 и 3, Δt < 50 ms.
2. Pi 5 сохраняет clip; судья листает кадры.
3. `final_time` + `final_source: photo`.

### 7.4. Управление с ноутбука (подготовка)

1. Подключение к WiFi Pi 5 (`timing-race` или LAN).
2. Создание Event, импорт CSV участников.
3. Формирование очереди heats, назначение bib на полосы.
4. Проверка `/api/status`: камера 120 fps, ESP32 online.

### 7.5. Управление со смартфона (на площадке)

1. Стартер открывает «Queue» → Activate следующий heat.
2. Экран «Start»: Arm → GO (или физические кнопки на Start Unit).
3. Live-таблица обновляется без перезагрузки страницы.
4. При споре судья открывает Photo на телефоне или планшете.

---

## 8. Спецификация железа

### 8.1. Central (Pi 5)

| Позиция | Qty |
|---------|-----|
| Raspberry Pi 5 8 GB | 1 |
| USB-UART адаптер (CH340 / CP2102) | 2 |
| Pi Camera Module 3 | 1 |
| Flat CSI-кабель 30–50 cm | 1 |
| microSD 64 GB A2 | 1 |
| БП 5 V **5 A** USB-C | 1 |
| Корпус IP65 + активный радиатор / вентилятор | 1 |
| Штатив, кронштейн (камера ⊥ линии) | 1 |
| LED-бар 12 V + рассеиватель | 1–2 |
| Powerbank 20 000 mAh (PD 5 V 3 A+) или 12 V агрегат | 1 |

### 8.2. Start Unit

| Позиция | Qty |
|---------|-----|
| ESP32-S3 DevKit (WiFi) | 1 |
| LED-матрица 8×8 или 4× 7-segment | 1 |
| Зуммер 5 V | 1 |
| Кнопки Arm / Start | 2 |
| Корпус IP65 | 1 |
| БП 5 V (USB или powerbank) | 1 |
| Кабель UART к Pi 5, 5–10 m (витая пара / мультикор) | 1 |

### 8.3. Finish Unit

| Позиция | Qty |
|---------|-----|
| ESP32-S3 DevKit | 1 |
| IR LED + фототранзистор + LM358 (на полосу) | 4 |
| Финишная рама (ПВХ / алюминий), задел под RFID-антенну | 1 |
| Белая линия 5 cm | — |
| Корпус IP65 | 1 |
| UART + GPIO к Pi 5, 3–5 m | 1 |

### 8.4. Общее

| Позиция | Qty |
|---------|-----|
| Powered USB-hub (для RFID / периферии) | 1 |
| Кабели USB-C, Dupont, витая пара | комплект |
| Ethernet-кабель (Pi 5 ↔ ноутбук, опционально) | 1 |
| Конуса, лента разметки | — |

### 8.6. Опция RS485 (фаза v2)

| Позиция | Qty |
|---------|-----|
| MAX485 / SN65HVD модуль | 2 |
| Витая пара в экране, 50–300 m | 1 |

---

### 8.7. Опция RFID (фаза 2)

| Позиция | Qty |
|---------|-----|
| UHF reader (R2000 / R500) | 1 |
| Patch-антенна 868 MHz | 1–2 |
| UHF-теги (шлем / номер) | N |

---

## 9. Программная часть

| Комponent | Стек |
|-----------|------|
| OS | Raspberry Pi OS Lite 64 |
| Backend | Python 3, FastAPI, SQLite, WebSocket, REST API |
| Video | `picamera2`, ring buffer, GPIO trigger |
| Frontend | HTMX / лёгкий SPA: **mobile** (starter, live) + **desktop** (editor, CSV) |
| Auth | PIN / role-based session (без внешних сервисов) |
| ESP32 finish | Arduino / ESP-IDF; ISR `micros()` + JSON UART |
| ESP32 start | Dual transport: UART (приоритет) / WiFi / RS485 (v2); буфер + replay |
| Сервисы | `timing.service`, `chrony`, `avahi` (mDNS) |
| USB gadget | `usb0` RNDIS (опционально, для прямого подключения ноутбука) |

**MVP v1:** CRUD heats с телефона/ПК, activate + arm/go, 4 lane IR, photo @ 120 fps, live WebSocket, CSV.  
**v2:** RFID, PDF протокол, OpenCV auto-line, push-уведомления PWA.

---

## 10. Смета (ориентир, ₽)

### Полный комплект (Pi 5 + старт + финиш)

| Статья | ₽ |
|--------|---|
| RPi 5 8 GB + Camera 3 + SD + БП + охлаждение | 16 000 – 22 000 |
| ESP32-S3 × 2 | 1 600 – 2 400 |
| IR × 4 + платы | 1 500 – 2 500 |
| Start UI (LED, зуммер, кнопки) | 800 – 1 500 |
| Финишная рама, боксы | 3 000 – 6 000 |
| Штатив, LED-подсветка | 2 000 – 5 000 |
| Кабели, hub, powerbank | 3 000 – 5 000 |
| **Итого** | **≈ 28 000 – 44 000** |
| **Рекомендуемый бюджет «под ключ»** | **≈ 33 000 – 38 000** |

### RFID (фаза 2)

| | ₽ |
|---|-----|
| Reader + антенна + 50 тегов | **≈ 18 000 – 35 000** |

### Сравнение с v0.2 (Zero + Pi 5)

| | v0.2 | **v0.3** |
|---|------|----------|
| Плат | 2 × RPi | **1 × RPi 5** |
| Бюджет | ~40–45k | **~33–38k** |
| Экономия | — | **~5–7k ₽** |

*Разброс ±20–30% в зависимости от Ali / локального рынка.*

---

## 11. Этапы разработки

| Этап | Срок | Результат |
|------|------|-----------|
| 1. Прототип финиша | 1–2 нед | 1 полоса IR → UART/GPIO → Pi 5 → CSV |
| 2. Старт + WiFi | 1–2 нед | Start → Pi 5; arm/go с web-UI |
| 3. Управление heats | 1–2 нед | CRUD, очередь, activate, роли, REST + WebSocket |
| 4. Pi 5 photo + UI | 1–2 нед | 120 fps, GPIO trigger, photo inline |
| 5. Полевой комплект | 1 нед | 4 полосы, корпуса, автономия 4 ч |
| 6. Тест-старт | 1 день | Сценарий секретарь + стартер + судья |
| 7. RFID | по готовности | Reader в раму, uid ↔ bib |

---

## 12. Риски

| Риск | Митигация |
|------|-----------|
| Pi 5 — single point of failure | ESP32 буферизует события; автосохранение SQLite; clip на SD |
| Перегрев Pi 5 (камера + WiFi + лето) | Активное охлаждение, тень, throttling monitor |
| Просадка 5 V | БП / powerbank **5 A**, короткий USB-C |
| Старт далеко от финиша | **RS485** (v2) или **WiFi** на Start ESP32; внешняя антенна |
| Потеря WiFi (UI / Start) | Finish всегда по UART; Start: UART или буфер + replay |
| Кабель через трассу | Защитный канал / лента; не на проезжей части |
| Rolling shutter | 120 fps; при необходимости — GS USB-камера |
| Ложные IR | Debounce 1–2 ms; двойной луч (v2) |
| Случайное управление с чужого телефона | PIN на WiFi AP + role PIN для arm/go |
| Потеря связи клиента mid-heat | Heat и события на Pi 5; reconnect WebSocket без потери данных |

---

## 13. Критерии приёмки MVP
- [ ] С ноутбука: импорт CSV, создание и редактирование heats, activate
- [ ] Со смартфона: arm/go, live-таблица, очередь heats
- [ ] 3 клиента одновременно без рассинхрона live
- [ ] Масс-старт и старт по одному — 10 заездов без сбоев
- [ ] 4 полосы финиша различаются корректно
- [ ] Pi 5: 120 fps ≥ 5 мин, clip по GPIO, IR ↔ frame привязаны
- [ ] Finish Unit: только UART + GPIO; Start Unit: UART **и** WiFi (оба режима проверены)
- [ ] `/api/status` показывает `finish_link=uart`, `start_link=uart|wifi`
- [ ] Start Unit по WiFi при расстоянии ≥ 30 m; по UART при кабеле 10 m
- [ ] Смартфон: live ≤ 1 с; фото-финиш в том же UI ≤ 3 с
- [ ] Экспорт CSV: `bib`, `lane`, `time`, `mode`, `final_source`
- [ ] Автономность 4 ч; работа без интернета
- [ ] После reboot — данные на SD сохранены

---

## 14. История версий
| Версия | Изменения |
|--------|-----------|
| 0.1 | Zero Hub, ESP32 start/finish, Pi 4 photo |
| 0.2 | Фото-финиш на RPi 5; Zero остаётся Hub |
| 0.3 | Zero убран; все функции на одном RPi 5; Start по WiFi |
| 0.4 | Управление Pi 5 с ПК и смартфона: роли, CRUD heats, API, команды arm/go |
| 0.4.1 | Убран обосновывающий раздел «Zero vs Pi 5»; перенумерация разделов |
| 0.4.2 | **§3.1 Связь Pi 5 ↔ ESP32:** провод (UART/RS485) + WiFi; Finish только провод |

---

## 15. Дальше (не в scope v0.4.2)

- Pinout ESP32 (start / finish) + GPIO Pi 5
- Формат UART / WiFi / RS485-пакетов, схема SQLite, OpenAPI spec
- Монтаж финишной рамы, калибровка IR, схема RS485-bus (v2)
